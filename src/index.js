import React from 'react';
import ReactDOM from 'react-dom/client';
import { AnimeShop } from './AnimeShop';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <React.StrictMode>
    <AnimeShop />
  </React.StrictMode>
);